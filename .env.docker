# Environment Configuration for Docker
PORT=3000

# Database Configuration
DB_HOST=postgres
DB_PORT=5432
DB_USER=nest_user
DB_PASSWORD=nest_password
DB_NAME=nest_db

# Redis Configuration
REDIS_HOST=redis
REDIS_PORT=6379

# JWT Secrets
JWT_SECRET=my-secret-change-in-production
REFRESH_TOKEN_SECRET=my-refresh-secret-change-in-production

# Frontend URL
FRONTEND_URL=http://localhost:3000

# Mail Configuration (Mailtrap for development)
MAIL_HOST=sandbox.smtp.mailtrap.io
MAIL_PORT=2525
MAIL_USER=d9172cb5ec0325
MAIL_PASS=5f67c440cfbf75

# Rate Limiting
THROTTLE_TTL=60000
THROTTLE_LIMIT=10

# Google OAuth (optional)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_CALLBACK_URL=
